
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.baria.mvp',
  appName: 'BARIA-MVP',
  webDir: '../web/dist', // uses the web build directly
  bundledWebRuntime: false
};

export default config;
